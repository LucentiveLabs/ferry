---
"@lucentive-labs/ferry": patch
---

Keep redacted output on the stream that supplied the final data, independent of stdout/stderr close order, and pin the release toolchain to patched js-yaml lines.
